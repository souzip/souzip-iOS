import UIKit

public extension UIImage {
    // MARK: - Navigation / Direction

    static let dsIconArrowLeft: UIImage = .iconArrowLeft

    static let dsIconChevronUp: UIImage = .iconChevronUp
    static let dsIconChevronDown: UIImage = .iconChevronDown
    static let dsIconChevronLeft: UIImage = .iconChevronLeft
    static let dsIconChevronRight: UIImage = .iconChevronRight

    // MARK: - Notification

    static let dsIconBell: UIImage = .iconBell
    static let dsIconBellActive: UIImage = .iconBellActive

    // MARK: - Media / Input

    static let dsIconCamera: UIImage = .iconCamera
    static let dsIconCancel: UIImage = .iconCancel

    // MARK: - Selection / State

    static let dsIconCheck: UIImage = .iconCheck
    static let dsIconCheckSelected: UIImage = .iconCheckSelected
    static let dsIconCheckCircleFilled: UIImage = .iconCheckCircleFilled

    // MARK: - Editing / Action

    static let dsIconEdit: UIImage = .iconEdit
    static let dsIconEditContained: UIImage = .iconEditContained
    static let dsIconPlusCircleFilled: UIImage = .iconPlusCircleFilled

    // MARK: - Reaction / Reward

    static let dsIconHeart: UIImage = .iconHeart
    static let dsIconHeartFilled: UIImage = .iconHeartFilled
    static let dsIconStar: UIImage = .iconStar
    static let dsIconGift: UIImage = .iconGift

    // MARK: - Information / Link

    static let dsIconInformationCircle: UIImage = .iconInformationCircle
    static let dsIconLinkSelected: UIImage = .iconLinkSelected

    // MARK: - Location / Map

    static let dsIconLocationTarget: UIImage = .iconLocationTarget
    static let dsIconLocationTargetActive: UIImage = .iconLocationTargetActive
    static let dsIconGlobe: UIImage = .iconGlobe
    static let dsIconPin: UIImage = .iconPin

    // MARK: - System / Utility

    static let dsIconSearch: UIImage = .iconSearch
    static let dsIconSettingBold: UIImage = .iconSettingBold
    static let dsIconTrash: UIImage = .iconTrash
    static let dsIconUserProfile: UIImage = .iconUserProfile
}
